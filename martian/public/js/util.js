const hexadecimals = [...Array(10).keys(), "A", "B", "C", "D", "E", "F"];
const selectors = {
  arrow: null,
};

const rotateState = {
  currPoint: 0,
  currDeg: 15,
};

export { hexadecimals, selectors, rotateState };
