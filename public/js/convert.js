const convertCommunication = (transceiverParts) => {
    const { 
        receiveContentHex, translateBtn, 
        sendContentHex, sendToEarthBtn,
    } = transceiverParts;
};

export default convertCommunication;